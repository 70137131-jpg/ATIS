ANPR sample images for local OCR smoke checks and threshold tuning.

Expected labels:

- `sample_plate_abc_1234.png` -> `ABC-1234`
- `sample_plate_lea_4455.png` -> `LEA-4455`
- `sample_plate_low_light_nha_908.png` -> `NHA-908`

These are synthetic fixtures, not production training data.
