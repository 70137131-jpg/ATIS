.DS_Store
.env
.venv/
venv/
venv312/
__pycache__/
*.py[cod]
.pytest_cache/
static/uploads/*
!static/uploads/.gitkeep
config/live_zones.json
instance/
# Large local datasets / archives. Keep these in external artifact storage, not Git.
ATIS_Dataset/
Tire Textures/
*.zip

# Scratch checkpoints created during evaluation/retraining are not release artifacts.
*.bak.pt
runs/**/weights/last*.pt
runs/**/weights/epoch*.pt

# coverage
.coverage
